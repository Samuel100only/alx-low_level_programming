#include "main.h"
/**
 * _atoi - convert the string to interger
 * @s: the string variable
 * Return: value of int s
 */
int _atoi(char *s)
{
	int x;
	int w;
	
	for (x = 0; *(s + x) != '\0'; x++)
	{
		if ( (*(s + x) = '-') && (57 >= *(s + x + 1) <= 48)
		{
		for (; 57 >= *(s + x + 1) <= 48; x++)
		{
		
		}
		}
	}
}

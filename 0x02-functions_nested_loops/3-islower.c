#include "main.h"
/**
 * _islower - return 1 if c is lower case or 0 if c is upper
 * Return: 1 or 0
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	else
	{	
	return (0);
	}
}

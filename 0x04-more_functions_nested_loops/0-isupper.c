#include "main.h"
/**
 * _isupper - return 1 if c is uppercase otherwise return 0
 * @c: the character to be checked
 * Return: 1 for uppercase 0 for lower
 */
int _isupper(int c)
{
	if (c >= 65 || c <= 90)
	{
	return (1);
	}
	else
	return (0);
}

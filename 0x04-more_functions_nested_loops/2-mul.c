#include "main.h"
/**
 * mul - multyply and return the result
 * @a: the character to be multiply
 * @b: the character to be multiply
 * Return: the result
 */
int mul(int a, int b)
{
	return (a * b);
}
